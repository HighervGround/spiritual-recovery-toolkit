📝 CREATE YOUR .env FILE
========================

To use Supabase backend, create a file named `.env` in the project root with the following content:

VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

Replace the placeholder values with your actual Supabase credentials from:
https://app.supabase.com/project/_/settings/api

⚠️ IMPORTANT: 
- Do NOT commit the .env file to git
- The .gitignore is already configured to exclude it
- Keep your keys secure!

If you don't set up Supabase, the app will automatically use localStorage instead.



